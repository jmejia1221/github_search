import React from 'react';

// CSS
import styles from './index.module.scss';

const Home = () => {
    return (
        <section className={styles.main}>
            hey
        </section>
    );
};

export default Home;