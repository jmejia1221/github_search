import React from 'react';

// Components
import Input from '../UI/Input';

const Search = () => {
    return (
        <div>
            <Input placeholder="Search" />
        </div>
    );
};

export default Search;